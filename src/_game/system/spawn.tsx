import { useFrame } from "@react-three/fiber";
import React from "react";

export default function spawn() {
  // check distance??
  // get All available items here
  // get Which items to spawn next:
  // drop
  // get probability to spawn -> low rate
  // get probability to
  // todo: differentiate RARITY spawn item
  // right now just spawn items all
  // useFrame()
}
